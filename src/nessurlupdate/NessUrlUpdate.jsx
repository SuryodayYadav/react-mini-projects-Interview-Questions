import { useEffect } from "react";

const NessUrlUpdate = () => {

    const url = "http://www.surya.com/customer/updateCustomer?region=US&discount=20"
    // axios.get(url, userService -> )
    // setResult(...)
    //
    // useEffect(() => {
    //     const region = URLSearchParams(url, "region");
    //     const discount = URLSearchParams(url, "dicount");
    //     const payload = {
    //         region,
    //         discount
    //     }
    //     //axios.post(payload, url)
    // }, [])  
    //JPA - > updateByRegionAndDiscount(String region, long disount)
    // @Query(Update Customer SELECT dicsount FROM CUSTOMER WHERE region = "US" AND discount="20" AND SET dicount = 30)
    // @ID @GenerationType.SEQUENCE
    // spring-secutiry
    // @Bean -> config -> @Configuration -> @Bean -> Security -> http.authorize("").csrf.auth.
    // @predistroy 
    // @Autowired 
    // @SpringBootAPplicatio -> 
    // @Scheduler(0, 0, 0, 9)
    // S -> SRP -> 
    // O -> OEC
    // L -> 
    // I -> 
    // D -> 
    // Callable -> 
    // Runnable -> 
    // Executor -> 
    // Thread Safe Collection -> ConccurrentHashMap, vector, 
    // Sync, Con -> 
    // @Transcation -> 
    // ACID -> A -> Atomicity
    // C -> COnsiti
    // Idempodency -> 
    // D -> 
    // pom.xml -> @Bean -> .postForObj (Multipart file) - .filers
    // @spri 
    return(
        <div>
            {XPathResult.map( )}
        </div>
    )

}

export default NessUrlUpdate;